[project]
name = "usmle-study-helper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Semere Wubshet", email = "80589097+SemereWubshet@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "datasets>=5.0.1",
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "uvicorn[standard]>=0.52.4",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "jinja2>=3.1.6",
    "matplotlib>=3.11.2",
    "pandas>=3.0.5",
]
